import { OrderChatroomPipe } from './order-chatroom.pipe';

describe('OrderChatroomPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderChatroomPipe();
    expect(pipe).toBeTruthy();
  });
});
