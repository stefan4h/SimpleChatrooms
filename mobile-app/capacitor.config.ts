import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simplechatrooms.app',
  appName: 'simple-chatrooms-mobile-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
